import React from "react";

const headers = () => {
    return(
        [
            {
                name: "project",
                label: "Projeto"
            },
            {
                name: "actions",
                label: "Ações"
            }
        ]
    )
}

export default headers();